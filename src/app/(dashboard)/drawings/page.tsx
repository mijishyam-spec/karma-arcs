import { PlaceholderPage } from "@/components/modules/PlaceholderPage";

export default function DrawingsPage() {
  return (
    <PlaceholderPage
      title="Drawings"
      description="Drawing register, revisions, and approval workflow."
      phase={3}
    />
  );
}
